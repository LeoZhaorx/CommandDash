# Contributing

感谢你愿意改进 CommandDash。

1. Fork 仓库并从 `main` 创建功能分支。
2. 保持改动聚焦，不要提交个人 `.command` 文件、运行数据或 `build/` 目录。
3. 运行 `./scripts/verify.sh`，确认通用构建、最低系统版本和 README 资源检查通过。
4. 在 Pull Request 中说明改动目的、行为变化和验证方式。

界面或功能变更应尽量附带脱敏后的真实截图。不要在截图中包含个人路径、端口、项目名称或密钥。
